strict digraph test {
    VideoReader -- PlateDetector;
    VideoReader -- CarDetector;
    VideoReader -- OcrReader;
    PlateDetector -- OcrReader;
    OcrReader -- VideoRender;
    PlateDetector -- VideoRender;
    CarDetector -- VideoRender;
}